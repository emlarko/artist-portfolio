<section class="container">

    <header><h1>{{artwork.title}}</h1></header>
      <img src="{{artwork.image_url}}" alt="{{artwork.description}}">
      <p>By: {{artwork.user.name}}</p>
    </div>

</section>