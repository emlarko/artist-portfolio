<!DOCTYPE html>
<html lang="en">

<head>
  <meta charset="utf-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>The Gallery</title>
   <link
    rel="stylesheet"
    href="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/css/bootstrap.min.css"
  />
<link href="https://fonts.googleapis.com/css2?family=Playfair+Display:ital@0;1&display=swap" rel="stylesheet"> 
  <link rel="stylesheet" href="/css/jass.css">
  <link rel="stylesheet" href="/css/style.css">
</head>

<body>

  <nav class="navbar navbar-expand-lg navbar-light bg-light">
    <div class="container-fluid">
        <a href="/" style="font-size: 30px; text-transform: uppercase; color: white;" class="navbar-brand">The Gallery</a>
        <button type="button" class="navbar-toggler" data-bs-toggle="collapse" data-bs-target="#navbarCollapse">
            <span class="navbar-toggler-icon"></span>
        </button>
        <div class="collapse navbar-collapse" id="navbarCollapse">
            <div class="navbar-nav" style="font-size: 18px">
                <a href="/" style="color: white;" class="nav-item nav-link">Home</a>
                <a href="/dashboard" style="color: white;" class="nav-item nav-link">Dashboard</a>
                {{#if logged_in}}
                <a style="color: white;" class="nav-item nav-link" href='/current-artworks'>My Artworks</a>
                {{/if}}
            </div>
            <div class="navbar-nav ms-auto"style="font-size: 18px">
              {{#if logged_in}}
                <a class="nav-item nav-link" style="color: white;" id="logout">Logout</a>
              {{else}}
                <a href="/login" style="color: white;" class="nav-item nav-link">Login</a>
              {{/if}}
            </div>
        </div>
    </div>
</nav>
  <main>
      {{{body}}}
  </main>
  <!-- If the user is logged in, load the JS script for when they logout -->
  {{#if logged_in}}
  <script src="/js/logout.js"></script>
  {{/if}}
</body>
<script src="https://cdn.jsdelivr.net/npm/bootstrap@5.0.2/dist/js/bootstrap.bundle.min.js"></script>
</html>
