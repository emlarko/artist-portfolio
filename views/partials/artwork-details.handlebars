<section class="card">
  <a href="/artwork/{{id}}">
    <header>{{title}}</header>

    <div class="card-body">
      <img src="/images/{{image_url}}" alt="{{description}}">
      <p>By: {{artwork.user.username}}</p>
    </div>
  </a>
</section>