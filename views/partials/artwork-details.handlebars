<section class="card">
  <a href="/artwork/{{id}}">
    <header>{{title}}</header>

    <div class="card-body">
      <img src="{{image_url}}" alt="{{description}}">
      <p>By: {{artwork.user.name}}</p>
    </div>
  </a>
</section>